class QueryKey {
  const QueryKey._();

  static const pOffset = 'page[offset]';
  static const pLimit = 'page[limit]';
  static const sort = 'sort';
  static const fRoute = 'filter[route]';
  static const fTrip = 'filter[trip]';
}
